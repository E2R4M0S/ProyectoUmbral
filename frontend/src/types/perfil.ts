export interface PerfilData {
  firstName: string;
  lastName: string;
  alias: string;
  email: string;
}

export interface UpdatePerfilRequest {
  firstName: string;
  lastName: string;
  alias: string;
}
